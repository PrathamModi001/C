#include<stdio.h>

int main(){
    int a=5, b=10;
    printf("The length of one of the sides is %d cm\n",a);
    printf("The length of one of the other side is %d cm\n",b);

    printf("So, the area of the corresponding rectangle is %d\n",a*b);
    return 0;
}